# Workflow: <name>

> Copy this file to a descriptive name (e.g. `scrape_website.md`) and fill it in.
> Workflows are plain-language SOPs. The Agent reads this, then runs the tools.

## Objective
What this workflow accomplishes, in one or two sentences.

## Inputs
- `input_name` (type) — description, where it comes from, required/optional.

## Tools used
- `tools/<script>.py` — what it does and how it's invoked, e.g.
  `python tools/<script>.py --arg value`

## Steps
1. Step one.
2. Step two.
3. ...

## Expected output
What the deliverable is and where it lands (cloud service, `.tmp/`, etc.).

## Edge cases & lessons learned
- Known failure modes, rate limits, timing quirks. Update this section as you learn.
