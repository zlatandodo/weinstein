# Workflows

Plain-language SOPs (Layer 1 of the WAT framework). Each file defines an
objective, required inputs, which tools to run, expected outputs, and how to
handle edge cases.

- Start new workflows from `TEMPLATE.md`.
- Keep them current: when you learn a constraint or a better method, update the
  relevant workflow.
- Don't overwrite or delete workflows without explicit approval.
